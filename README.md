# CropwayGIS Frontend

Standalone Next.js frontend for CropwayGIS V1.

## Commands

```bash
pnpm install
pnpm dev
pnpm lint
pnpm test
pnpm build
```

Set `NEXT_PUBLIC_API_BASE_URL` in `.env.local` if the backend is not running on `http://127.0.0.1:8000`.

